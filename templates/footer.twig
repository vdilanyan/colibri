		</div> {# END wrapper #}
		<footer>
			<div class="container text-center">
				{% if footer.social_links %}
					<ul class="social-links flex-container">
						{% for social in footer.social_links %}
							<li>
								<a href="{{ social.link }}" target="_blank">{{ social.name }}</a>
							</li>
						{% endfor %}
					</ul>
				{% endif %}
			</div>
			<p class="copy text-center">Copyright {{ "now"|date('Y') }}. {{ site.name }}. All rights reserved</p>
		</footer>
		{{ function('wp_footer') }}
	</body>
</html>